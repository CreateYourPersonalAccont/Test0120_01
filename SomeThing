全局数据对象具有文件作用域，有效性从定义处直到本文件结束，其后函数都可以直接使用。
若包含全局数据对象定义的文件被其他文件包含，则其作用域扩展到宿主文件中，不要在通文件中定义全局数据对象。别人在他的文件中包含了头文件，作用于就扩大了。
全局变量不能同名，哪怕是在不同的文件中。
函数原型作用于仅持续到此函数原型结束，不会延续到函数实现。
全局解析操作符：：。
类的存储类与生存期。

static修饰全局变量用来限定作用域，仅在本文件内部，其他文件不可见。

free(p)之后应紧跟着p=null;因为p所指的内存区域虽然失效了，但是指针仍然有效。
